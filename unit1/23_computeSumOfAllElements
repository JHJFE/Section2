function computeSumOfAllElements(arr) {
  // TODO: 여기에 코드를 작성합니다.
  let add = function (acc, cur){
    let nAcc = acc + cur;
    return nAcc
  }
  
  let result = arr.reduce(add,0);

  return result;
}
