function getLengthOfLongestElement(arr) {
  // 여기에 코드를 입력하세요
  if(arr.length === 0){
    return 0;
  }
  
  let len =arr.map(function (str){
    return str.length;
  })
  
  return Math.max(...len);
  
}